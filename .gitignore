# ---------- Node / Next.js ----------
node_modules/
.next/
out/
build/
dist/
*.tsbuildinfo
next-env.d.ts

# Package manager logs / caches
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/
.yarn/
.npm/

# ---------- Python ----------
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
.eggs/
pip-log.txt
pip-delete-this-directory.txt

# Virtual environments
.venv/
venv/
env/
ENV/
.python-version

# Test / coverage / type-check caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
.nox/
.coverage
.coverage.*
htmlcov/
coverage.xml
*.cover

# Jupyter
.ipynb_checkpoints/

# ---------- Env / secrets ----------
.env
.env.*
!.env.example
!.env.sample
!.env.local.example
*.pem
*.key

# ---------- IDE / editor ----------
.vscode/
.idea/
*.swp
*.swo
*.sublime-project
*.sublime-workspace
.cursorrules

# ---------- OS ----------
.DS_Store
Thumbs.db
desktop.ini
$RECYCLE.BIN/

# ---------- Logs / temp ----------
*.log
*.bak
*.bak.json
logs/
tmp/
temp/
.cache/

# ---------- Vercel ----------
.vercel

# ---------- Project-specific ----------
Plant.id_api_v3.txt
data/

# NCPMS OpenAPI 매뉴얼 (로컬 참고 자료 — 코드/런타임 비의존)
npms_api_manual/

# 워킹트리 임시 검증 스크립트 (정식 회귀 도구는 scripts/run_eval.py)
_validate_*.py

# ---------- Eval dataset images (수집 산출물; metadata/labels.json만 추적) ----------
test_data/inaturalist_candidates/images/
test_data/wikimedia_candidates/images/
test_data/plantvillage_50/images/
test_data/self_captured/images/
test_data/main_eval/images/
test_data/moneyplant_candidates/images/
test_data/plantvillage_source/

# 평가셋 이미지는 로컬만 (토큰·용량·라이선스 — labels.json의 image_path 참조로 충분)
test_data/**/*.jpg
test_data/**/*.jpeg
test_data/**/*.png

# ---------- 내부 작업 기록 · 측정 산출물 (로컬 보관, GitHub 제외) ----------
# 라운드 작업 지시/결과 프롬프트, 컨텍스트 덤프, 설계 노트 — 공개 얼굴엔 README만.
docs/
# 측정 출력 JSON(앵커·_archive 포함). baseline.json은 로컬 보존(무수정).
eval/
# Claude Code 프로젝트 메모(내부 작업 메모리).
CLAUDE.md
# Claude Code 자동 운영 키트(에이전트·커맨드) + 무과금 점검 보고서 — 로컬 작업 도구, 공개 얼굴엔 제외.
.claude/
reports/

# ---------- RAG 원본 PDF (raw 참고 자료 — 로컬 보관) ----------
/FSA-6116.pdf
/pnhouseplantproblems.pdf

# ---------- 아카이브 · 라운드 진단용 일회성 스크립트 (로컬 보관) ----------
scripts/_archive/
scripts/diagnostics/

# ---------- 드래프트 · 백업 라벨 (정본은 labels.json만 추적) ----------
test_data/**/*_draft.json
test_data/**/*.bak.json
