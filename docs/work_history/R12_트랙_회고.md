# R12 트랙 회고 (R12_0 ~ R12d-1)

- **기간/라운드**: 2026-06-07 ~ 06-08, 5 서브라운드 (R12_0·R12b·R12c1·R12c1a·R12d1). R12a는 별도 트랙으로 진행 후 🟡 종료(커밋 348aca4, `R12a_result.md` 참조).
- **목적**: R11 v2(FP14/FN1) 기반으로 건조·영양부족 변별 실패와 cause-status 모순을 진단·교정하고, surface 패치를 빼기로 분리해 본질 기여를 측정. 현 활성 앵커 `after_acc_r12d1_relabeled.json`(acc 62.86%/FP13/FN0)로 수렴.

## 주요 변경·결과 (deep — 미해결 변수 보존)

### R12_0 (readonly 진단)
- 3영역 진단: (A) RAG는 영문 query_en 쿼리(ada-002 비 cross-lingual). (B) 가드는 비건강→건강 1방향뿐, 006 FN=가장자리 갈변 cosmetic + top1 abiotic→건강(하엽 위치 무시). (C) 건조 전용카드 1장뿐, 6건 중 5건 top_10 미진입.
- **C.4 NEW**: cause-status 모순(003 cause "수분 부족"·008 "과도한 건조"인데 enum 병해/영양) → R12b 레버리지로 식별.
- probe 스크립트 재사용 가능(`R12_PROBE_EMBED`).

### R12b (cause-status 정합 룰)
- prompts.py 정합 self-check 1블록(가건조·나과습·다영양·라병해 매핑 + 다중원인 우선 + 모순 금지 + cosmetic escape). 커밋 210b254.
- **실측 5/6 PASS**: post.fn 1→0(recall 1.0)·FP14 불변·건조 발화 첫 1건(005)·건강행 건조 0.
- **한계**: ① recall 복구는 006 analyze 변동 의존(룰 성과 아님 → R12a 필요) ② 003·008 cause는 수분 좁혔으나 status 병해 위반(generate escalation 편향) ③ 건조 정답 6중 1.

### R12c1 (RAG 콘텐츠 건조카드)
- 건조카드 6장 신설(dry_supplement, cc_authored) + abiotic-water 택소노미 + 재분류 3장. build PASS: verify_dry_top10 6/6(R12_0 0장→2~6장 진입, 검색 층위B 해결).
- 남은 변수: generate가 status=건조로 옮기는지(하류).

### R12c1a (변별 진단)
- 진짜 건조 vs 정상 노화는 **종으로만 변별**(결론C). 카드 negative 감별 비권장.

### R12d-1 (빼기 라운드 — surface 분리)
- 제거: R10 황화룰(ccf5b8b) + status_hint dead metadata(639b76c). 실측 PASS: post.fn 1→0·FP 17→15·건조 TP 4→5.
- **분리 3종**: ① status_hint=dead code 확정(graph.py read 0) ② healthy→건조 FP 진짜 범인=abiotic-water 카드 본문/타입(유지한 본질과 동근) ③ FN=0은 006 analyze 운 의존.
- **R10 황화룰**: 중복 화면 패치로 본질 기여 ~0 → 빼도 recall·건조 신호 손실 없음. 잡은 healthy→건조 FP는 유지한 abiotic-water 카드가 보완(②의 동근), 제거해도 손실 없음. (개별 FP 효과는 status_hint와 묶여 제거돼 분리 측정 불가; FP 17→15는 두 제거의 합산치.)
- 이후 FP15 relabel rescore(`generate_정밀화_회고.md` 참조)로 FP 15→13 → 현 앵커 `after_acc_r12d1_relabeled.json` 확정.

## 교훈
- cause-status 모순은 generate escalation 편향의 표면 — 입력(프롬프트) 설득으로 status는 안 옮겨짐(R12b 한계②). → R12a 출력 우회(가드 위치 veto)로 이어짐. **[R12a 결과·종료] veto는 FP-중립이나 효과 미입증(타깃 006이 토큰 "아래쪽" 대신 "아래가"를 추출 → 미발동). recall 안정은 veto가 아니라 analyze 증상 내용 + 기존 가드가 만든 것. veto는 안전핀으로 유지, 근본 해결은 ⇒ 모델 교체로 이관(R12a_result.md).**
- status_hint·R10 황화룰 빼기 근거는 CLAUDE.md **§7.3·§7.4** 참조.
- "추가만으로 안 나아진다" → 정기적 빼기 라운드: **§7.5** 참조.

## 원본 파일 (→ `_archive/원본/R12/`)
- R12_0_readonly_diagnosis_task.md, R12_0_readonly_diagnosis.md
- R12b_cause_status_consistency_task.md, R12b_cause_status_consistency_result.md
- R12c1_rag_content_task.md
- R12c1a_discrimination_diagnosis_task.md, R12c1a_discrimination_diagnosis.md
- R12d1_remove_surface_task.md, R12d1_remove_surface_result.md
